[project]
name = "l1nkzip-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rich",
    "httpx",
    "typer",
]

[dependency-groups]
dev = [
    "mypy>=1.20.1",
    "ruff>=0.12.7",
    "pytest>=8.0.0",
]
