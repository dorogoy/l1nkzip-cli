[project]
name = "l1nkzip-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rich",
    "httpx",
    "typer",
]

[dependency-groups]
dev = [
    "mypy>=1.17.1",
    "ruff>=0.15.10",
    "pytest>=8.0.0",
]
