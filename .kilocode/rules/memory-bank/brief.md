# Brief

This the CLI brother project of [L1nZip](https://github.com/dorogoy/l1nkzip), which is a URL shortener API. The CLI is meant to provide a command-line interface for interacting with the l1nkzip API, allowing users to shorten URLs, manage their links, and access all this information directly from the terminal.
